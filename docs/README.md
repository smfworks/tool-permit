# docs screenshots

Captured from the live compositor:

- `app-desktop.png` — builder + permit (lab sandbox)
- `permit-card.png` — isolated card (`?sample=lab-sandbox&shot=card`)
- `app-mobile.png` — 390px viewport
